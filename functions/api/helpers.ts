export function authenticateUser(username: string, password: string): boolean {
  // Placeholder: replace with actual authentication logic, e.g., database lookup
  return username === 'test' && password === 'password';
}